< false | `set` {
    'erp': | `schema` < {} {}< | `constrained` ~ {
        'Name': < | `single line` ~ >
        'Email': < | `single line` ~ >
        'Phone': < | `single line` ~ >
        'Address': < | `multi line` ~ >
        'Description': < | `multi line` ~ >
        'SKU': < | `single line` ~ >
        'Notes': < | `multi line` ~ >
    } {
        'Money': < | `number`< | `exact`< *  2 | `integer` ~ > > >
        'Quantity': < | `number`< | `exact`< _ | `natural` ~ > > >
        'Percentage': < | `number`< | `exact`< *  2 | `integer` ~ > > >
        'Date': < | `date` ~ >
        'Positive Integer': < | `number`< | `exact`< _ | `positive natural` ~ > > >
    } > {
        'ERP System': < | `group` {
            'company': < _ | `component`< | `internal` 'Company Info' _ > >
            'master data': < _ | `component`< | `internal` 'Master Data' _ > >
            'sales orders': < _ | `dictionary`< | `component`< | `internal` 'Sales Order' _ > > >
            'purchase orders': < _ | `dictionary`< | `component`< | `internal` 'Purchase Order' _ > > >
            'invoices': < _ | `dictionary`< | `component`< | `internal` 'Invoice' _ > > >
        } >
        'Master Data': < | `group` {
            'customers': < _ | `dictionary`< | `component`< | `internal` 'Customer' _ > > >
            'vendors': < _ | `dictionary`< | `component`< | `internal` 'Vendor' _ > > >
            'products': < _ | `dictionary`< | `component`< | `internal` 'Product' _ > > >
            'payments': < _ | `dictionary`< | `component`< | `internal` 'Payment' _ > > >
        } >
        'Company Info': < | `group` {
            'name': < _ | `text` | `global` 'Name' >
            'address': < _ | `text` | `global` 'Address' >
            'email': < _ | `text` | `global` 'Email' >
            'phone': < _ | `text` | `global` 'Phone' >
            'tax id': < _ | `text` | `global` 'Name' >
        } >
        'Customer': < | `group` {
            'name': < _ | `text` | `global` 'Name' >
            'contact': < _ | `component`< | `internal` 'Contact Info' _ > >
            'billing address': < _ | `text` | `global` 'Address' >
            'shipping address': < _ | `optional` | `text` | `global` 'Address' >
            'credit limit': < _ | `simple` | `global` 'Money' >
            'payment terms': < _ | `component`< | `internal` 'Payment Terms' _ > >
            'status': < _ | `state`< {
                'active': < _ _ | `nothing` ~ >
                'inactive': < _ _ | `nothing` ~ >
                'suspended': < _ _ | `group` {
                    'reason': < _ | `text` | `global` 'Notes' >
                } >
            } _ > >
        } >
        'Vendor': < | `group` {
            'name': < _ | `text` | `global` 'Name' >
            'contact': < _ | `component`< | `internal` 'Contact Info' _ > >
            'address': < _ | `text` | `global` 'Address' >
            'payment terms': < _ | `component`< | `internal` 'Payment Terms' _ > >
            'tax id': < _ | `optional` | `text` | `global` 'Name' >
        } >
        'Contact Info': < | `group` {
            'email': < _ | `text` | `global` 'Email' >
            'phone': < _ | `text` | `global` 'Phone' >
            'contact person': < _ | `optional` | `text` | `global` 'Name' >
        } >
        'Payment Terms': < | `state`< {
            'immediate': < _ _ | `nothing` ~ >
            'net days': < _ _ | `group` {
                'days': < _ | `simple` | `global` 'Positive Integer' >
            } >
            'end of month': < _ _ | `group` {
                'plus days': < _ | `simple` | `global` 'Quantity' >
            } >
        } _ > >
        'Product': < | `group` {
            'name': < _ | `text` | `global` 'Name' >
            'sku': < _ | `text` | `global` 'SKU' >
            'description': < _ | `text` | `global` 'Description' >
            'type': < _ | `state`< {
                'physical': < _ _ | `group` {
                    'stock level': < _ | `simple` | `global` 'Quantity' >
                    'reorder point': < _ | `simple` | `global` 'Quantity' >
                } >
                'service': < _ _ | `nothing` ~ >
                'digital': < _ _ | `nothing` ~ >
            } _ > >
            'unit price': < _ | `simple` | `global` 'Money' >
            'cost': < _ | `simple` | `global` 'Money' >
            'tax rate': < _ | `simple` | `global` 'Percentage' >
            'active': < _ | `state`< {
                'yes': < _ _ | `nothing` ~ >
                'no': < _ _ | `group` {
                    'discontinued date': < _ | `simple` | `global` 'Date' >
                } >
            } _ > >
        } >
        'Sales Order': < | `group` {
            'order number': < _ | `text` | `global` 'Name' >
            'customer': < _ | `reference`<<< | `internal` 'Master Data' ~ >< [ | `group` 'customers'] ~ > > | `selected`< ~ | `acyclic` ~ _ > > >
            'order date': < _ | `simple` | `global` 'Date' >
            'status': < _ | `state`< {
                'draft': < _ _ | `nothing` ~ >
                'confirmed': < _ _ | `nothing` ~ >
                'shipped': < _ _ | `group` {
                    'tracking number': < _ | `text` | `global` 'Name' >
                    'shipped date': < _ | `simple` | `global` 'Date' >
                } >
                'delivered': < _ _ | `group` {
                    'delivery date': < _ | `simple` | `global` 'Date' >
                } >
                'cancelled': < _ _ | `group` {
                    'reason': < _ | `text` | `global` 'Notes' >
                } >
            } _ > >
            'items': < _ | `list`< | `component`< | `internal` 'Order Line Item' _ > _ > >
            'notes': < _ | `optional` | `text` | `global` 'Notes' >
        } >
        'Purchase Order': < | `group` {
            'order number': < _ | `text` | `global` 'Name' >
            'vendor': < _ | `reference`<<< | `internal` 'Master Data' ~ >< [ | `group` 'vendors'] ~ > > | `selected`< ~ | `acyclic` ~ _ > > >
            'order date': < _ | `simple` | `global` 'Date' >
            'expected delivery': < _ | `optional` | `simple` | `global` 'Date' >
            'status': < _ | `state`< {
                'pending': < _ _ | `nothing` ~ >
                'approved': < _ _ | `nothing` ~ >
                'received': < _ _ | `group` {
                    'received date': < _ | `simple` | `global` 'Date' >
                } >
                'cancelled': < _ _ | `nothing` ~ >
            } _ > >
            'items': < _ | `list`< | `component`< | `internal` 'Order Line Item' _ > _ > >
        } >
        'Order Line Item': < | `group` {
            'product': < _ | `reference`<<< | `internal` 'Master Data' ~ >< [ | `group` 'products'] ~ > > | `selected`< ~ | `acyclic` ~ _ > > >
            'quantity': < _ | `simple` | `global` 'Quantity' >
            'unit price': < _ | `simple` | `global` 'Money' >
            'discount': < _ | `optional` | `group` {
                'amount': < _ | `state`< {
                    'fixed': < _ _ | `simple` | `global` 'Money' >
                    'percentage': < _ _ | `simple` | `global` 'Percentage' >
                } _ > >
            } >
        } >
        'Invoice': < | `group` {
            'invoice number': < _ | `text` | `global` 'Name' >
            'related order': < _ | `state`< {
                'sales order': < _ _ | `text` | `global` 'Name' >
                'purchase order': < _ _ | `text` | `global` 'Name' >
                'none': < _ _ | `nothing` ~ >
            } _ > >
            'issue date': < _ | `simple` | `global` 'Date' >
            'due date': < _ | `simple` | `global` 'Date' >
            'items': < _ | `list`< | `component`< | `internal` 'Order Line Item' _ > _ > >
            'status': < _ | `state`< {
                'draft': < _ _ | `nothing` ~ >
                'sent': < _ _ | `group` {
                    'sent date': < _ | `simple` | `global` 'Date' >
                } >
                'paid': < _ _ | `group` {
                    'payment': < _ | `text` | `global` 'Name' >
                } >
                'overdue': < _ _ | `nothing` ~ >
                'cancelled': < _ _ | `nothing` ~ >
            } _ > >
        } >
        'Payment': < | `group` {
            'payment date': < _ | `simple` | `global` 'Date' >
            'amount': < _ | `simple` | `global` 'Money' >
            'payment method': < _ | `state`< {
                'cash': < _ _ | `nothing` ~ >
                'check': < _ _ | `group` {
                    'check number': < _ | `text` | `global` 'Name' >
                } >
                'bank transfer': < _ _ | `group` {
                    'reference number': < _ | `text` | `global` 'Name' >
                } >
                'credit card': < _ _ | `group` {
                    'last four digits': < _ | `text` | `global` 'Name' >
                    'authorization code': < _ | `text` | `global` 'Name' >
                } >
            } _ > >
            'reference': < _ | `optional` | `text` | `global` 'Notes' >
        } >
    } | `constrained`<< {
        'ERP System': < ~ | `local`< {
            'Company Info': << | `internal` 'Company Info' ~ > | `required` ~ >
            'Customer': << | `internal` 'Customer' ~ > | `required` ~ >
            'Product': << | `internal` 'Product' ~ > | `required` ~ >
            'Vendor': << | `internal` 'Vendor' ~ > | `required` ~ >
            'Sales Order': << | `internal` 'Sales Order' ~ > | `required` ~ >
            'Purchase Order': << | `internal` 'Purchase Order' ~ > | `required` ~ >
            'Invoice': << | `internal` 'Invoice' ~ > | `required` ~ >
            'Payment': << | `internal` 'Payment' ~ > | `required` ~ >
            'Contact Info': << | `internal` 'Contact Info' ~ > | `required` ~ >
            'Payment Terms': << | `internal` 'Payment Terms' ~ > | `required` ~ >
            'Order Line Item': << | `internal` 'Order Line Item' ~ > | `required` ~ >
        } {} > ~ >
        'Master Data': < ~ | `local`< {
            'Customer': << | `internal` 'Customer' ~ > | `required` ~ >
            'Vendor': << | `internal` 'Vendor' ~ > | `required` ~ >
            'Product': << | `internal` 'Product' ~ > | `required` ~ >
            'Payment': << | `internal` 'Payment' ~ > | `required` ~ >
            'Contact Info': << | `internal` 'Contact Info' ~ > | `required` ~ >
            'Payment Terms': << | `internal` 'Payment Terms' ~ > | `required` ~ >
        } {} > ~ >
        'Company Info': < ~ | `local`< {} {} > ~ >
        'Customer': < ~ | `local`< {
            'Contact Info': << | `internal` 'Contact Info' ~ > | `required` ~ >
            'Payment Terms': << | `internal` 'Payment Terms' ~ > | `required` ~ >
        } {} > ~ >
        'Vendor': < ~ | `local`< {
            'Contact Info': << | `internal` 'Contact Info' ~ > | `required` ~ >
            'Payment Terms': << | `internal` 'Payment Terms' ~ > | `required` ~ >
        } {} > ~ >
        'Contact Info': < ~ | `local`< {} {} > ~ >
        'Payment Terms': < ~ | `local`< {} {} > ~ >
        'Product': < ~ | `local`< {} {} > ~ >
        'Sales Order': < ~ | `local`< {
            'Master Data': << | `internal` 'Master Data' ~ > | `required` ~ >
            'Order Line Item': << | `internal` 'Order Line Item' ~ > | `required` ~ >
        } {} > ~ >
        'Purchase Order': < ~ | `local`< {
            'Master Data': << | `internal` 'Master Data' ~ > | `required` ~ >
            'Order Line Item': << | `internal` 'Order Line Item' ~ > | `required` ~ >
        } {} > ~ >
        'Order Line Item': < ~ | `local`< {
            'Master Data': << | `internal` 'Master Data' ~ > | `required` ~ >
        } {} > ~ >
        'Invoice': < ~ | `local`< {
            'Master Data': << | `internal` 'Master Data' ~ > | `required` ~ >
            'Order Line Item': << | `internal` 'Order Line Item' ~ > | `required` ~ >
        } {} > ~ >
        'Payment': < ~ | `local`< {} {} > ~ >
    } > {
        'ERP System': < ~ | `group` {
            'company': < ~ | `component`< ~ | `internal` 'Company Info' ~ * < *  {} *  {} > {} > >
            'master data': < ~ | `component`< ~ | `internal` 'Master Data' ~ * < *  {
                'Customer': | `parameter` 'Customer'
                'Vendor': | `parameter` 'Vendor'
                'Product': | `parameter` 'Product'
                'Payment': | `parameter` 'Payment'
                'Contact Info': | `parameter` 'Contact Info'
                'Payment Terms': | `parameter` 'Payment Terms'
            } *  {} > {} > >
            'sales orders': < ~ | `dictionary`< ~ | `component`< ~ | `internal` 'Sales Order' ~ * < *  {
                'Master Data': | `required` < | `sibling` 'master data'< [] ~ > ~ >
                'Order Line Item': | `parameter` 'Order Line Item'
            } *  {} > {} > _ > >
            'purchase orders': < ~ | `dictionary`< ~ | `component`< ~ | `internal` 'Purchase Order' ~ * < *  {
                'Master Data': | `required` < | `sibling` 'master data'< [] ~ > ~ >
                'Order Line Item': | `parameter` 'Order Line Item'
            } *  {} > {} > _ > >
            'invoices': < ~ | `dictionary`< ~ | `component`< ~ | `internal` 'Invoice' ~ * < *  {
                'Master Data': | `required` < | `sibling` 'master data'< [] ~ > ~ >
                'Order Line Item': | `parameter` 'Order Line Item'
            } *  {} > {} > _ > >
        } >
        'Master Data': < ~ | `group` {
            'customers': < ~ | `dictionary`< ~ | `component`< ~ | `internal` 'Customer' ~ * < *  {
                'Contact Info': | `parameter` 'Contact Info'
                'Payment Terms': | `parameter` 'Payment Terms'
            } *  {} > {} > _ > >
            'vendors': < ~ | `dictionary`< ~ | `component`< ~ | `internal` 'Vendor' ~ * < *  {
                'Contact Info': | `parameter` 'Contact Info'
                'Payment Terms': | `parameter` 'Payment Terms'
            } *  {} > {} > _ > >
            'products': < ~ | `dictionary`< ~ | `component`< ~ | `internal` 'Product' ~ * < *  {} *  {} > {} > _ > >
            'payments': < ~ | `dictionary`< ~ | `component`< ~ | `internal` 'Payment' ~ * < *  {} *  {} > {} > _ > >
        } >
        'Company Info': < ~ | `group` {
            'name': < ~ | `text` ~ >
            'address': < ~ | `text` ~ >
            'email': < ~ | `text` ~ >
            'phone': < ~ | `text` ~ >
            'tax id': < ~ | `text` ~ >
        } >
        'Customer': < ~ | `group` {
            'name': < ~ | `text` ~ >
            'contact': < ~ | `component`< ~ | `internal` 'Contact Info' ~ * < *  {} *  {} > {} > >
            'billing address': < ~ | `text` ~ >
            'shipping address': < ~ | `optional`< {} | `text` ~ > >
            'credit limit': < ~ | `simple` ~ >
            'payment terms': < ~ | `component`< ~ | `internal` 'Payment Terms' ~ * < *  {} *  {} > {} > >
            'status': < ~ | `state`< ~ {
                'active': < {} | `nothing` ~ >
                'inactive': < {} | `nothing` ~ >
                'suspended': < {} | `group` {
                    'reason': < ~ | `text` ~ >
                } >
            } > >
        } >
        'Vendor': < ~ | `group` {
            'name': < ~ | `text` ~ >
            'contact': < ~ | `component`< ~ | `internal` 'Contact Info' ~ * < *  {} *  {} > {} > >
            'address': < ~ | `text` ~ >
            'payment terms': < ~ | `component`< ~ | `internal` 'Payment Terms' ~ * < *  {} *  {} > {} > >
            'tax id': < ~ | `optional`< {} | `text` ~ > >
        } >
        'Contact Info': < ~ | `group` {
            'email': < ~ | `text` ~ >
            'phone': < ~ | `text` ~ >
            'contact person': < ~ | `optional`< {} | `text` ~ > >
        } >
        'Payment Terms': < ~ | `state`< ~ {
            'immediate': < {} | `nothing` ~ >
            'net days': < {} | `group` {
                'days': < ~ | `simple` ~ >
            } >
            'end of month': < {} | `group` {
                'plus days': < ~ | `simple` ~ >
            } >
        } > >
        'Product': < ~ | `group` {
            'name': < ~ | `text` ~ >
            'sku': < ~ | `text` ~ >
            'description': < ~ | `text` ~ >
            'type': < ~ | `state`< ~ {
                'physical': < {} | `group` {
                    'stock level': < ~ | `simple` ~ >
                    'reorder point': < ~ | `simple` ~ >
                } >
                'service': < {} | `nothing` ~ >
                'digital': < {} | `nothing` ~ >
            } > >
            'unit price': < ~ | `simple` ~ >
            'cost': < ~ | `simple` ~ >
            'tax rate': < ~ | `simple` ~ >
            'active': < ~ | `state`< ~ {
                'yes': < {} | `nothing` ~ >
                'no': < {} | `group` {
                    'discontinued date': < ~ | `simple` ~ >
                } >
            } > >
        } >
        'Sales Order': < ~ | `group` {
            'order number': < ~ | `text` ~ >
            'customer': < ~ | `reference`< ~ | `selected`< ~< | `acyclic` | `resolved dictionary`<< | `parameter` 'Master Data'< [ | `group` 'customers'] ~ > ~ > ~ > ~ > {} > > >
            'order date': < ~ | `simple` ~ >
            'status': < ~ | `state`< ~ {
                'draft': < {} | `nothing` ~ >
                'confirmed': < {} | `nothing` ~ >
                'shipped': < {} | `group` {
                    'tracking number': < ~ | `text` ~ >
                    'shipped date': < ~ | `simple` ~ >
                } >
                'delivered': < {} | `group` {
                    'delivery date': < ~ | `simple` ~ >
                } >
                'cancelled': < {} | `group` {
                    'reason': < ~ | `text` ~ >
                } >
            } > >
            'items': < ~ | `list`< ~ | `component`< ~ | `internal` 'Order Line Item' ~ * < *  {
                'Master Data': | `parameter` 'Master Data'
            } *  {} > {} > _ > >
            'notes': < ~ | `optional`< {} | `text` ~ > >
        } >
        'Purchase Order': < ~ | `group` {
            'order number': < ~ | `text` ~ >
            'vendor': < ~ | `reference`< ~ | `selected`< ~< | `acyclic` | `resolved dictionary`<< | `parameter` 'Master Data'< [ | `group` 'vendors'] ~ > ~ > ~ > ~ > {} > > >
            'order date': < ~ | `simple` ~ >
            'expected delivery': < ~ | `optional`< {} | `simple` ~ > >
            'status': < ~ | `state`< ~ {
                'pending': < {} | `nothing` ~ >
                'approved': < {} | `nothing` ~ >
                'received': < {} | `group` {
                    'received date': < ~ | `simple` ~ >
                } >
                'cancelled': < {} | `nothing` ~ >
            } > >
            'items': < ~ | `list`< ~ | `component`< ~ | `internal` 'Order Line Item' ~ * < *  {
                'Master Data': | `parameter` 'Master Data'
            } *  {} > {} > _ > >
        } >
        'Order Line Item': < ~ | `group` {
            'product': < ~ | `reference`< ~ | `selected`< ~< | `acyclic` | `resolved dictionary`<< | `parameter` 'Master Data'< [ | `group` 'products'] ~ > ~ > ~ > ~ > {} > > >
            'quantity': < ~ | `simple` ~ >
            'unit price': < ~ | `simple` ~ >
            'discount': < ~ | `optional`< {} | `group` {
                'amount': < ~ | `state`< ~ {
                    'fixed': < {} | `simple` ~ >
                    'percentage': < {} | `simple` ~ >
                } > >
            } > >
        } >
        'Invoice': < ~ | `group` {
            'invoice number': < ~ | `text` ~ >
            'related order': < ~ | `state`< ~ {
                'sales order': < {} | `text` ~ >
                'purchase order': < {} | `text` ~ >
                'none': < {} | `nothing` ~ >
            } > >
            'issue date': < ~ | `simple` ~ >
            'due date': < ~ | `simple` ~ >
            'items': < ~ | `list`< ~ | `component`< ~ | `internal` 'Order Line Item' ~ * < *  {
                'Master Data': | `parameter` 'Master Data'
            } *  {} > {} > _ > >
            'status': < ~ | `state`< ~ {
                'draft': < {} | `nothing` ~ >
                'sent': < {} | `group` {
                    'sent date': < ~ | `simple` ~ >
                } >
                'paid': < {} | `group` {
                    'payment': < ~ | `text` ~ >
                } >
                'overdue': < {} | `nothing` ~ >
                'cancelled': < {} | `nothing` ~ >
            } > >
        } >
        'Payment': < ~ | `group` {
            'payment date': < ~ | `simple` ~ >
            'amount': < ~ | `simple` ~ >
            'payment method': < ~ | `state`< ~ {
                'cash': < {} | `nothing` ~ >
                'check': < {} | `group` {
                    'check number': < ~ | `text` ~ >
                } >
                'bank transfer': < {} | `group` {
                    'reference number': < ~ | `text` ~ >
                } >
                'credit card': < {} | `group` {
                    'last four digits': < ~ | `text` ~ >
                    'authorization code': < ~ | `text` ~ >
                } >
            } > >
            'reference': < ~ | `optional`< {} | `text` ~ > >
        } >
    } > >
} >